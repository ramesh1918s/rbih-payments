apiVersion: v1
kind: Service
metadata:
  name: payment-gateway-nodeport
  namespace: payments
  labels:
    app: payment-gateway
spec:
  selector:
    app: payment-gateway
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
